# EPICS 核心概念

> **阅读时间**: 30分钟
> **难度**: ⭐⭐⭐⭐☆
> **重要性**: ⭐⭐⭐⭐⭐

## 概述

理解这4个核心概念是学习EPICS的关键：
1. **PV** (Process Variable) - 数据点
2. **Record** - 数据容器
3. **IOC** (Input/Output Controller) - 运行环境
4. **Channel Access** - 通信协议

## 1. PV (Process Variable)

### 什么是PV？

**PV是EPICS中的数据点**，类似于：
- 网址URL (全局唯一标识)
- 变量名 (在程序中)
- 数据库表中的一行

### PV命名规则

```
系统:子系统:设备:参数
│    │      │    │
│    │      │    └─ 具体参数 (Temp, Voltage, Position)
│    │      └────── 设备名称 (Sensor1, Motor2)
│    └─────────── 子系统 (Vacuum, RF, BPM)
└──────────────── 系统名称 (iLinac, SPEAR, ALS)
```

**示例**:
```
iLinac_007:BPM14And15:RF3Amp     # 射频3号通道振幅
SPEAR:VacuumSystem:Gauge1:Pressure  # 真空计1压力
ALS:Motor:X:Position              # X轴电机位置
```

### PV的特点

1. **全局唯一**: 整个EPICS网络中PV名称不能重复
2. **网络可访问**: 任何客户端都能通过名称访问
3. **大小写敏感**: `RF3Amp` ≠ `rf3amp`
4. **命名规范**: 建议使用有意义的名称

### PV命名最佳实践

```
✅ 好的命名:
iLinac_007:BPM14:XPos           # 清晰、有层次
LAB:TempSensor1:Reading         # 含义明确

❌ 不好的命名:
abc123                          # 无意义
x                               # 太短
VeryLongPV_WithManyWords_ThatIsHardToType  # 太长
```

## 2. Record

### 什么是Record？

**Record是PV的实现**，包含：
- **值** (Value)
- **字段** (Fields)
- **处理逻辑** (Processing Logic)

### Record类比

把Record想象成一个**对象**（面向对象编程）：

```c
// 类比：C语言结构体
struct TemperatureSensor {
    double value;          // 当前温度
    char*  units;          // 单位 (摄氏度)
    int    precision;      // 精度
    double high_limit;     // 高限
    double low_limit;      // 低限
    void (*read)();       // 读取函数
    void (*alarm_check)(); // 告警检查
};
```

### Record类型

EPICS提供50+种Record类型，常用的包括：

| 类型 | 全称 | 用途 | 示例 |
|------|------|------|------|
| **ai** | Analog Input | 模拟输入 | 温度、电压 |
| **ao** | Analog Output | 模拟输出 | 设定值 |
| **bi** | Binary Input | 二进制输入 | 开关状态 |
| **bo** | Binary Output | 二进制输出 | 开关控制 |
| **calc** | Calculation | 计算 | 公式计算 |
| **waveform** | Waveform | 波形/数组 | 示波器数据 |
| **stringin** | String Input | 字符串输入 | 设备名称 |

### Record字段

每个Record有数十个字段，常用的包括：

```
record(ai, "TEMP:Sensor1") {
    field(VAL,  "25.3")      # Value - 值
    field(DESC, "室温传感器")  # Description - 描述
    field(SCAN, "1 second")  # Scan - 扫描周期
    field(EGU,  "C")         # Engineering Units - 工程单位
    field(PREC, "1")         # Precision - 精度
    field(HOPR, "100")       # High Operating Range - 上限
    field(LOPR, "0")         # Low Operating Range - 下限
    field(HIHI, "80")        # High High Alarm - 高高告警
    field(HIGH, "60")        # High Alarm - 高告警
    field(HSV,  "MAJOR")     # High Severity - 高告警严重性
}
```

### Record处理流程

```
触发条件 (SCAN)
    ↓
读取输入 (INP字段)
    ↓
设备支持 (DSET)
    ↓
更新VAL字段
    ↓
单位转换
    ↓
告警检查
    ↓
触发前向链接 (FLNK)
    ↓
通知监控者 (CA)
```

## 3. IOC (Input/Output Controller)

### 什么是IOC？

**IOC是运行EPICS数据库的程序**，类似于：
- Web服务器 (提供网络服务)
- 数据库服务器 (管理数据)
- 驱动程序 (控制硬件)

### IOC的组成

```
┌─────────────────────────────────┐
│            IOC进程               │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │  Database (数据库)        │  │
│  │  - Records                │  │
│  │  - 处理逻辑               │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │  Channel Access Server    │  │
│  │  - 网络服务               │  │
│  │  - 客户端通信             │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │  Device Support           │  │
│  │  - 硬件抽象层             │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │  Driver                   │  │
│  │  - 硬件访问               │  │
│  └───────────────────────────┘  │
└──────────┬──────────────────────┘
           │
      [ 硬件设备 ]
```

### IOC的功能

1. **数据采集**: 从硬件读取数据
2. **控制输出**: 向硬件写入命令
3. **数据处理**: 执行计算和逻辑
4. **网络服务**: 提供Channel Access服务
5. **告警处理**: 监测异常状态
6. **数据归档**: 记录历史数据

### IOC的类型

| 类型 | 描述 | 示例 |
|------|------|------|
| **软IOC** | 纯软件，测试用 | softIoc |
| **嵌入式IOC** | 运行在嵌入式系统 | VxWorks IOC |
| **Linux IOC** | 运行在Linux服务器 | BPMIOC |

### IOC启动流程

```bash
./st.cmd                         # 1. 执行启动脚本
    ↓
dbLoadDatabase("xxx.dbd")        # 2. 加载数据库定义
    ↓
registerRecordDeviceDriver()     # 3. 注册驱动
    ↓
dbLoadRecords("xxx.db")          # 4. 加载数据库实例
    ↓
iocInit()                        # 5. 初始化IOC
    ↓
[IOC Shell]                      # 6. 进入交互shell
```

## 4. Channel Access (CA)

### 什么是Channel Access？

**Channel Access是EPICS的网络协议**，用于：
- 读取PV值
- 写入PV值
- 监控PV变化
- 获取PV元数据

### CA的工作方式

```
客户端                           IOC
  │                               │
  │ ─────── 搜索PV ─────────────> │
  │ <────── PV地址 ─────────────  │
  │                               │
  │ ─────── 建立连接 ───────────> │
  │ <────── 连接确认 ───────────  │
  │                               │
  │ ─────── 读取请求 ───────────> │
  │ <────── 返回数据 ───────────  │
  │                               │
  │ ─────── 订阅监控 ───────────> │
  │ <────── 值变化通知 ─────────  │
  │ <────── 值变化通知 ─────────  │
  │         ...                   │
```

### CA端口

- **5064** (UDP): 用于PV搜索
- **5065** (TCP): 用于数据传输

### CA工具

```bash
# 读取PV
caget TEMP:Sensor1
# 输出: TEMP:Sensor1  25.3

# 写入PV
caput TEMP:Setpoint 30
# 输出: Old : TEMP:Setpoint  25
#       New : TEMP:Setpoint  30

# 监控PV
camonitor TEMP:Sensor1
# 输出: TEMP:Sensor1  2025-11-07 10:30:45.123 25.3
#       TEMP:Sensor1  2025-11-07 10:30:46.123 25.4
#       ...

# PV信息
cainfo TEMP:Sensor1
# 输出: TEMP:Sensor1
#       State:          connected
#       Host:           192.168.1.100:5064
#       Data type:      DBR_DOUBLE
#       Element count:  1
```

## 概念关系图

```
┌─────────────────────────────────────────────────────┐
│                    EPICS 系统                        │
└─────────────────────────────────────────────────────┘
                        │
        ┌───────────────┴───────────────┐
        │                               │
┌───────▼────────┐             ┌────────▼────────┐
│     IOC-1      │             │     IOC-2       │
│  ┌──────────┐  │             │  ┌──────────┐   │
│  │ Record-A │  │             │  │ Record-C │   │
│  │ Record-B │  │             │  │ Record-D │   │
│  └──────────┘  │             │  └──────────┘   │
│       ↑        │             │       ↑         │
│  [ 硬件-1 ]    │             │  [ 硬件-2 ]     │
└────────┬───────┘             └────────┬────────┘
         │                              │
         │     Channel Access (网络)     │
         │                              │
    ┌────┴──────────────────────────────┴────┐
    │                                         │
┌───▼────────┐  ┌──────────┐  ┌──────────┐   │
│ Client-1   │  │ Client-2 │  │ Client-3 │   │
│ (caget)    │  │ (Python) │  │ (CSS)    │   │
└────────────┘  └──────────┘  └──────────┘   │
                                              │
          PV访问: "IOC-1:Record-A"            │
                                              │
└─────────────────────────────────────────────┘
```

## 实践示例

### 创建一个简单的数据库

```
# temp.db
record(ai, "LAB:Temp1") {
    field(DESC, "实验室温度")
    field(VAL,  "22.5")
    field(EGU,  "C")
    field(PREC, "1")
}

record(calc, "LAB:TempF") {
    field(DESC, "华氏温度")
    field(INPA, "LAB:Temp1")
    field(CALC, "A*9/5+32")
    field(EGU,  "F")
    field(PREC, "1")
}
```

### 运行IOC

```bash
softIoc -d temp.db
```

### 访问PV

```bash
# 读取摄氏温度
caget LAB:Temp1
# 输出: LAB:Temp1  22.5

# 读取华氏温度
caget LAB:TempF
# 输出: LAB:TempF  72.5

# 修改摄氏温度
caput LAB:Temp1 25
# LAB:TempF 自动更新为 77.0
```

## 关键要点总结

### PV
- **定义**: 数据点的名称
- **特点**: 全局唯一，网络可访问
- **示例**: `iLinac_007:BPM14:XPos`

### Record
- **定义**: PV的实现（值+逻辑）
- **类型**: ai, ao, calc, waveform等
- **字段**: VAL, SCAN, DESC等

### IOC
- **定义**: 运行数据库的程序
- **功能**: 数据采集、控制、网络服务
- **示例**: BPMIOC

### Channel Access
- **定义**: 网络通信协议
- **工具**: caget, caput, camonitor
- **端口**: 5064 (UDP), 5065 (TCP)

## 常见问题

### Q1: PV和Record有什么区别？
**A**: PV是名字，Record是实体。
- PV: `LAB:Temp1` (字符串)
- Record: ai类型的Record，有VAL、SCAN等字段

### Q2: 一个IOC可以有多少个Record？
**A**: 理论上无限，实际受限于：
- 内存（每个Record约1-2KB）
- CPU（扫描负载）
- 典型: 1000-10000个Record

### Q3: Channel Access和HTTP有什么区别？
**A**:
- CA: 实时、低延迟、订阅-推送
- HTTP: 请求-响应、延迟较高

### Q4: 为什么需要Device Support？
**A**: 分离抽象和实现
- Record: 抽象接口（ai、ao）
- Device Support: 具体硬件实现

## 下一步

现在你理解了核心概念，可以：

1. **创建第一个IOC** → [03-your-first-ioc.md](./03-your-first-ioc.md)
2. **深入Record类型** → [04-record-types.md](./04-record-types.md)
3. **学习CA工具** → [06-ca-tools.md](./06-ca-tools.md)

## 延伸阅读

- [EPICS Application Developer's Guide](https://epics.anl.gov/base/R3-15/6-docs/AppDevGuide.pdf)
- [Channel Access Reference](https://epics.anl.gov/base/R3-15/6-docs/CAref.html)

---

**记住**: 理解这4个核心概念是掌握EPICS的关键！花时间消化这些内容是值得的。🎯
